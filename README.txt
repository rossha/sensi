In order to see this example work, you must host the files and navigate to index.html
At this point, using Chrome's Developer Tools (or similar) you can view the network traffic 
	as well as the response objects from SignalR

We have CORS enabled, but allow http://localhost
